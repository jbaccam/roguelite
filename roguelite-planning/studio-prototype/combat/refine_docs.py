from pathlib import Path
p=Path('roguelite-planning/studio-prototype/combat/README.md');s=p.read_text(encoding='utf-8').replace('1.05 seconds, with 0.18s windup, 0.24s strike, and 0.28s recovery','0.8 seconds, with 0.12s windup, 0.16s strike, and 0.18s recovery').replace('The blade points along combat-local +Y.','The blade points along combat-local +Y, with a further user-requested 180-degree facing flip around Y.').replace('The katana attacks from the right slot. Opposite-side targets wait until reachable.','The right slot is its idle/home position. The katana targets enemies on every side, takes a fast outside arc to an attack position, aims and slashes, then returns home. This brief targeted attack repositioning is not continuous orbiting.').replace('Final functional test before accessory/trail refinement had no console errors.','The functional test before the final four-direction refinement had no console errors.')
p.write_text(s,encoding='utf-8')
p=Path('roguelite-planning/MASTER_GAME_DESIGN.md');s=p.read_text(encoding='utf-8').replace('25 damage, 1.05-second cadence','25 damage, 0.8-second cadence');s+='\nKatana targeting clarification: the fixed slot is its resting position, not a targeting restriction. It rapidly flips/aims toward enemies in any direction and may briefly reposition around the outside of the avatar for a clear slash, then returns home. Blade facing reversed 180 degrees per user feedback.\n';p.write_text(s,encoding='utf-8')
p=Path('roguelite-planning/FLOATING_WEAPON_PRESENTATION.md');s=p.read_text(encoding='utf-8');s+='\nKatana refinement: fixed idle slot, full-direction target acquisition. It quickly turns/flips toward the target and takes a short outside repositioning arc when necessary, rather than limiting attacks to one side. It returns to the same idle slot; no continuous 360-degree orbit.\n';p.write_text(s,encoding='utf-8')
